# Root-level, read by docker-compose itself (not by either service).
# Optional: only needed to point the built frontend at a backend on a
# different origin than the one nginx proxies to by default. See
# frontend/.env.example for why this is a build ARG, not a runtime var.
# VITE_API_URL=http://localhost:8000
