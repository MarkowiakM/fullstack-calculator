# Optional. Left unset, the API client calls a relative /api path, which
# works both via Vite's dev-server proxy (local `npm run dev`) and via
# nginx's reverse proxy (docker compose) — no CORS, no absolute URL needed.
# Only set this to point the frontend at a backend on a different origin.
# VITE_API_URL=http://localhost:8000
